# Hyrax Implementation Worklog — 2026-07-25

## Headline: VNCCS emotion suite COMPLETE — 4 operators × 157 emotions (~3,140 sprites)

Generation ran 2026-07-25 ~00:47 → ~19:50 PDT on the ComfyUI LXC (192.168.0.161,
3 instances: A4000 + 2× 5060 Ti, Anima base). tai/rei/nei/mai all at 157/157.
**Originals remain on .161** (`/opt/ComfyUI/output/VNCCS/Characters/<Name>/Sprites/Original/<emotion>/`,
5 sprites per emotion); only reviewed copies are pulled into the repo for the VN
(hyrax-assets/essence/frames/). Instances shut down after completion (GPUs idle);
restart with `start_all_gpu.sh` on .161.

### Generation-incident log (for future runs)

- `--host` flag parsed but never applied in `vnccs_queue.py` → all 4 scripts posted
  to :8188; 11 duplicate chunk-1 submissions cleaned up. Fixed + `save_state` made
  merge-safe, then made per-operator (stale snapshots resurrected hand edits).
- 5× host-level OOM kills of ComfyUI instances (3 × ~17 GB RSS vs 64 GB). Partial
  chunks salvaged from disk each time (state seeded per completed emotion).
  Root fix: LXC hotplugged to 96 GB RAM + 32 threads (~07:20); zero kills after.
- tai's workflow export had `widget_data.character_name: "Mai"` → tai's sprites
  saved into Mai's folder (node 404 drives appearance, node 405 drives save path).
  Verified via contact-sheet review that no Tai-looking sprites contaminated Mai's
  final outputs; tai's state reset to 0 and regenerated cleanly.
- Fleet consolidated to 2 instances mid-run (RAM), then back to 3 after the bump.
- Final 31 mai emotions split manually across 3 lanes to finish in ~1 h.
- Last emotion rendered: `exhausted`. Fitting.

## Living HQ (goal: verify + work-order habit — completed)

- HQ is the default landing (localStorage `hyrax-home`, `?panel=hq` honored).
- War-room strip: kanban running/blocked totals + per-operator chips, click →
  native kanban panel. Presence now exposes `currentTask {id,title}`
  (ready+running, ranked active-claim > running > ready).
- Chibis placed by real activity (`ACTIVITY_ROOM`), 30 s visibility-gated
  presence refresh, time-of-day tint, activity animations (reduced-motion safe).
- Operator card → standard chat (same session, no dupes); chibi → VN.
- Work-order rule live in all four real SOUL.md files (backup
  `20260725-193243-pre-work-order-rule`); `kanban` toolset was already armed.
  Rule text + evidence: docs/gestalt-vn/WORK_ORDER_HABIT.md (gitignored — `git add -f`).
- Proof: `tests/browser_living_hq.py` (22 checks) + `tests/browser_vn_interactions.py`.

## VN interaction fixes (user dogfooding, round 1)

- Loading-loop on chibi A→B→A re-entry: stale signature cache said no-op while
  DOM was empty; stage now trusts DOM over cache (vnStage.js).
- Dialogue opens scrolled to latest message (+2.5 s settle-pin vs resize bounce).
- Desktop sprite zoom: 1610×3840 sprites scaled to stage width (~4×). Desktop now
  a centered 4:5 column anchored top (VN half-cut). Fallback-portrait bleed fixed.
- Emotion jolt: expression-family change → bounce/shake/tilt/lean/settle on the
  sprite, reduced-motion gated.

## VN polish (user feedback, round 2)

- Per-frame calibration: `scripts/calibrate_frame_crops.py` (alpha bbox →
  `assets.display {scale, focusX, objectPositionY}` in frames.registry.json,
  schema-validated pass-through in api/hyrax_essence.py). All four operators now
  frame head+upper-body consistently. Re-run after new sprites land.
- Dialogue default height ~30 % viewport (was ~62 %); drag-resize persists per
  operator; collapse to composer-only bar; splitter keyboard-accessible.
- Verification: `tests/browser_vn_polish.py` 8/8; 282 pytest green.

## dagoth-ur overhaul (backups: -opsec, -meme-rewrite, -admin-allowlist,
  -group-admin-fix, -tone-nerd, -nsfw-tone)

- OPSEC hard rule added: never reveal other profiles/infra (he leaked "other
  profiles" in Discord; root cause was no rule, not the word "sister").
- Persona rewritten: full Dagoth-Ur meme god, games/server subject matter (dev
  jargon banned unless asked), NSFW permitted with hard lines.
- Authz: admin commands DM-only for `68554430370283520` + asslord
  (`1448598625209811004`); group channel = interaction-only (`group_allow_admin_from: []`);
  informational (game server info/searches) open to everyone.

## ARDY / Phase-6 research convergence

- Graded Kimi's VNCCS→VRM research doc against the first-party audit
  (`ardy-bridge/RESEARCH_RECONCILIATION.md`): 18 verified, 5 disputed, 5 gaps.
  Key traps catalogued: Core27 `LeftLeg`=knee (SOMA=thigh), w-first quats,
  smooth_root_pos is an alias in ARDY, demo exports pickle not NPZ, yaw
  double-apply (exorcist candidate), matrix major-order transpose bug in the
  sample retargeter.
- GPT deep-research caught two errors everyone else missed: shoulders parent
  `Spine3` (our own audit doc had it wrong — corrected with dated note), and the
  `Matrix4.fromArray` column-major trap (would have inverted every rotation).
- Strategy recorded (ARDY_VRM_ARCHITECTURE.md addendum): canonical layer speaks
  semantic tokens only; Core27 = today's compatibility adapter; SOMA77 =
  tomorrow's canonical, buildable offline today via Kimodo-SOMA; ARDY-SOMA plugs
  in later. Pipeline code files patched and syntax-verified.

## Control-plane audit (parked, documented)

`gestalt-control-plane/docs/RESURRECTION_AUDIT_2026-07-25.md`: gateway healthy
after restart (boot race had cached all profiles unsafe → 503s); 39/39 tests;
three hard-broken endpoints and dead Plane remnants catalogued; decision made to
consolidate on the WebUI instead (HQ-first landing is the result).

## Verification assets

- dogfood-output/{living-hq,work-orders,vn-fixes,vn-polish}/ — screenshot evidence.
- tests/browser_{living_hq,vn_interactions,vn_polish}.py — reusable harnesses.

## Uncommitted at time of writing

api/hyrax_essence.py, frames.registry.json, static/hyrax/{UI-LAYOUT.md,
bootstrap.js, hq.js, hyrax.css, vn/vnDialogue.js, vn/vnShell.js, vn/vnStage.js},
tests/*, scripts/calibrate_frame_crops.py, dogfood-output/*, hyrax-3d/REsearch/*.
