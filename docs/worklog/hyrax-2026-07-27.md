# Hyrax Implementation Worklog — 2026-07-27

## Headline: the division is fully alive — mood in their voices, memory in their tools, upstream current

## Mood-to-voice layer (the day's big build)

- essenced: `tone_by_state` in rules.json (weary-gentle/clipped/irritable/
  bright/precise/warm + per-operator defaults; rei defaults `dry`),
  `presentation.tone` stamped in derived_state.json.
- sister-essence plugin: generic `pre_llm_call` hook (dispatcher for future
  injectors — Phase 6 ARDY, work helpers) injects a compact state line per
  turn: `[essence] mood/energy/stress/tone — color your voice, never quote
  numbers`. Fail-closed identity, read-only, kill-switch
  (ESSENCE_CONTEXT_HOOK=0). Hard proof: line present in mai's api_content.
- Sentiment-enriched turn deltas: praise/criticism/question/long/short
  signal table in rules.json, personality-weighted, journaled.
- Live proof (mai, 3 turns): calm → warm/unhurried; stressed → still gentle
  without steering; tired+hook → "Tired, if I'm honest… slower today.
  Gentler." Tone embodied, numbers never quoted. 196 essenced + 39 plugin
  tests green. Committed (api/hyrax_essence.py tone in derivedState).

## VN pose/expression tuning

- Pose: sitting now requires energy<0.3 OR resting>2h (was: any rest →
  everyone "laying down"). New `resting_above_seconds` condition.
- Conversing upgrades neutral expression via engaged_chain (light-smile/
  smile/calm/observant/alert/thinking per operator enum) — no more
  blank-stare-when-chatting. 160 essenced tests green.

## Outreach — organic fires confirmed end-to-end

- HERMES_WEBUI_PASSWORD missing from essenced unit → both lanes failed
  closed (journaled). Fixed via /root/.config/essenced/essenced.env (600) +
  EnvironmentFile. tai+nei delivered on both lanes an hour later — first
  fully organic proactive messages.
- tai: "peeks in 🎀 Hey Josh~ Just felt like reaching out…" / nei: quieter
  deliberate warmth. Personalities audible, no templates.
- One transient: mai fire 409'd while user mid-conversation (guard allowed
  at 47min idle; stream active at turn start seconds later) — safe withhold,
  no cap consumed, retries on refill.

## Cognee (memory platform) — pilot → fleet rollout

- venv /opt/cognee (cognee 1.4.0 + cognee-mcp 0.5.5), kuzu+lancedb embedded,
  fastembed local embeddings. LLM extraction: `openai/deepseek-v4-flash` via
  opencode-go + `LLM_INSTRUCTOR_MODE=json_mode` (required — endpoint rejects
  tool-calling; bare model string fails without openai/ prefix).
- systemd cognee-mcp on 127.0.0.1:8001/mcp. Rolled out to all 10 profiles
  (4 sisters + hyrax-os + 4 hx-* + dagoth-ur) via mcp_servers config.
- Usage rules added to all four operators' reasoning-protocol skill
  (recall BEFORE history questions/prior work; remember AFTER decisions/
  root-causes/preferences; dedup; no secrets).
- Verified: MCP remember+recall round-trip; recall returns graph answers.
  Note: embedded store is single-writer — all access via MCP, not direct SDK.
- **Linear removed** (nei's MCP entry was the only one; plane MCP was
  disabled yesterday). RIP.

## Role skills deployed (all four profiles, all surfaces)

- Shared: reasoning-protocol (spec→execute→check→polish→harden).
- rei-qa-protocol (evidence standard, [QA] filing template for tai),
  tai-build-protocol (smallest slice, evidence completion reports),
  nei-knowledge-protocol ([SPEC] shape, continuity hygiene),
  mai-support-protocol ([TRIAGE] intake, bug/build cycles).
- DIVISION_CYCLES.md documents the bug cycle (mai→rei→tai→rei-attest→mai)
  and build cycle (nei→tai→rei→mai).
- Rei's first QA pass (t_645e32cf): skills shaped output (verdict-first,
  severity tables, self-correction between passes) but 2/3 "blocking"
  findings were probe artifacts — tuning note: verify by interaction before
  declaring dead. Her 4 real findings fixed (tech drawer, pose reactivity,
  markdown, expression enum fallbacks).

## Mnemosyne

- mai added to mnemosyne_sleep_all.sh (was "all 3 profiles" — she was
  missing consolidation entirely). Now all 4.

## Upstream + runtime

- Merged upstream/master → v0.52.152 (18 behind / 57 ahead). One conflict:
  SSE terminal-event constants (ours _RUN_TERMINAL_EVENTS vs upstream
  SSE_RELAY_CLOSE_EVENTS — identical sets, adopted upstream's).
  338 hyrax tests + regression suites green.
- hermes update → v0.19.0 (desktop-UI changes only; plugin hook verified).
- **Recurrent VN "conflict" root-caused: agent_runtime_stale.** After
  `hermes update`, the WebUI's agent runtime is stale and EVERY turn 409s
  fleet-wide, masked by the VN as a generic conflict bubble. Fix: restart
  hermes-webui.service (verified 200 on all four operators).
  **Runbook: always restart hermes-webui after hermes update.**
  Future fix: VN should surface the real reason vs generic conflict.

## Next work period — open items

- Watch: outreach tuning (accumulator cadence, any spam/over-quiet),
  cognee tool invocation by operators (recall/remember in tool details),
  expression/pose feel after tuning.
- Fix: VN conflict bubble should surface actual reason (stale-runtime vs
  active-stream).
- Phase D1 (bounded autonomy, shadow mode) — after ~1 week of clean
  outreach journals.
- Whims layer + Mai's LXC playground as first autonomy lease class.
- Voice-layer follow-ups: Discord/CLI hook verification (same path,
  unproven); mai latency on heavy cron-context turns.
- Hygiene: rei's Discord 403 scope, matrix homeserver 502 storm, WebUI
  password rotation (plaintext in unit, 0.0.0.0 bind).
- ARDY/Phase 6 — parked, docs ready (ARDY_OUTPUT_CONTRACT, reconciliation,
  research corrections), GPU fleet idle.
