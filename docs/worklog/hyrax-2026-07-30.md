# Hyrax Implementation Worklog — 2026-07-30

## Headline: SOMA calibration became a product, then reached the normal viewer

## SOMA77 converter freeze and baseline

- Documented the exact retained hierarchy, collapsed-parent semantics,
  transform conventions, root ownership, compatibility alias, and numerical
  tolerances.
- Locked converter expansion behind failing contract evidence.
- Generated signed evidence from the real 150-frame Kimodo capture, including
  empty failure fields and retained-contract/source signatures.
- Captured the existing hardcoded Tai output over fixed rest, Core27,
  Kimodo, locomotion, and torso/arm samples, twice, with complete local/world
  transforms.

## Calibration Studio vertical slice

- Added strict engine-neutral Motion IR, Avatar Rig IR, and Calibration Profile
  contracts.
- Built VRM/GLB/FBX import boundaries, skeleton inspection, semantic mapping,
  clickable bone points, automatic rest-pose fitting, manual rotation gizmos
  and sliders, root/scale calibration, motion preview, deterministic
  validation, and profile export/import.
- Kept camera state stable while calibration controls are edited.
- Expanded authoring from the 22-control compatibility rig to a standardized
  54-control VRM view with body, finger, and eye roles.
- Added synchronized SOMA and frozen Legacy Tai references for calibration and
  regression comparison.

## Grounding and qualification

- Added explicit Core27-to-SOMA77 qualification adaptation for the three-part
  `.96` turn capture.
- Iterated from vertical foot drift to contact-aware grounded feet, level-foot
  orientation, heading locks, bounded pelvis compensation, and temporal IK.
- Final qualification passed with 8 left and 9 right contact transitions,
  `0.023397199 m` maximum pelvis lowering, approximately `1e-9 m` position
  residual, and at most approximately `1.7e-6°` orientation residual.
- Preserved the rule that IK corrects validated calibration; it does not hide
  bad mapping, rest, basis, hierarchy, scale, or root decisions.

## Normal viewer profile consumption

- Added a `config` picker to `debug/ardy.html`.
- Added the qualified Tai Studio artifact to the existing profile selector.
- Added strict schema/status validation, raw avatar hashing, exact normalized
  rig-signature matching, and visible fail-closed errors.
- Added both lossless SOMA77 and explicit Core27 motion adaptation.
- Added deterministic replay from frame zero on seeks for stateful ground/IK
  correction.
- Preserved the old profile and hardcoded reference paths.
- Added tests proving validated-profile playback is deterministic and draft
  profiles cannot execute.
- Resolved the real Tai asset to the expected signed 54-control rig while
  preserving its viewer display offset.
- Browser-verified both the built-in query path and the actual config file
  picker at desktop and narrow viewport widths; the imported profile replayed
  arbitrary frames with no page or console errors.

## Evidence and checks

- Foundation commit: `f6d39d60`
- Qualified runtime profile:
  `hyrax-3d/calibration-studio/evidence/tai.humanoid54.foot-ik.validated.json`
- Targeted viewer consumer tests: pass
- Real Tai asset hash and rig extraction: pass
- Known unrelated blocker: the pre-existing `VRMUtils.combineSkeletons`
  three-vrm type/API mismatch remains out of scope.
