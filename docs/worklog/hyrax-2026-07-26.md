# Hyrax Implementation Worklog — 2026-07-26

## Headline: the operators are alive — feel (A), show (B), reach out (C), all live

## essenced Phase A (Hermes-side, deterministic core)

- New package `/root/.hermes/essenced/` (stdlib-only): kanban + session
  watchers (read-only), data-driven rules engine (rules.json), decay ticker,
  per-operator `derived_state.json` (schema v2, per-field provenance),
  `journal.jsonl`. Zero LLM in the loop.
- systemd `essenced.service`: enabled, restart=always, `--check-staleness`.
- Two-lane covenant: sister's `state.json` stays hers (self-report);
  essenced writes only `derived_state.json` + journals. Verified by mtime
  diffs across every run.
- Proof: seeded kanban claim→complete cycle → spec'd deltas in nei's journal;
  restart continuity (no duplicate events); 66 package tests green.
- Bug found by the proof: decay snap erased small event bumps in 15s → fixed
  (sub-resolution steps advance silently instead of teleporting to baseline).

## Phase A QA sweep

- FAIL found + fixed: `KANBAN_DB` resolved to a nonexistent per-profile path
  (api/config rewrites HERMES_HOME before hyrax_routes imports) — presence
  kanban/currentTask/war-room silently returned zeros in production.
  `_fleet_kanban_db()` fix; verified live (nei blocked:1 visible).
- Cleanup: kanban cursor prunes done >7d (mai 189→11), startup journal noise,
  sceneIntent unified to HQ room ids, dead conversing-long rule and
  --decay-interval flag removed.

## Phase B (eb3f60a3) — derived presentation drives the VN

- Presence derivedState block carries poseIntent/sceneIntent; hq.js feeds
  them into essence.state; VN stage swaps pose variants + backgrounds from
  essenced. Conversation expression precedence: session > fresh derived >
  keyword stopgap (demoted). Fail-closed everywhere.

## ds4-flash optimization (backup 20260726-172902)

- hyrax-os unbricked (was pointed at api.deepseek.com with opencode-go key).
- All 5 profiles: explicit openrouter/deepseek-v4-flash fallback (tai/rei had
  none). hx-* chains reordered (rate-limited copilot demoted).
- reasoning_effort xhigh → high on the four sisters (latency lever).
- hermes-router disabled (unused; would strip tool defs). Dead weight removed:
  rei stale custom providers, plane MCP (all profiles), rei DEEPSEEK_API_KEY.

## Phase C — proactive outreach (goal, 3 phases + 3 adversarial QA gates)

- C1: wants engine (social/purpose/stimulation) + 7-check policy gate
  (per-profile enabled default-off, lanes, quiet hours 23:00–08:00, daily cap
  2, cooldowns 3h/6h, per-operator shyness bars, live-conversation guard).
  Dry-run journals only.
- C2: mai's first real proactive message (her model, her voice, system note
  the only essenced-authored text). VN-turn delivery lane.
- C3: Discord DM lane (direct Discord API, per-operator bot tokens, token
  never logged). One want = one turn = one fire_id across both lanes.
- QA gates caught: fail-open config parsing (fixed strict), force-fire
  interleave guard (now enforced), cold-start lastInteractionAt seeding.
- **Live now: dry_run=false, all four operators enabled on both lanes.**
  First organic fires expected after midnight (mai capped from proof fires).
- Provenance: in-band `[essenced:outreach]` marker (metadata-level flagging
  would need upstream change — parked).

## VN bug-squash (live dogfooding)

- Stuck second message: SSE serves one run per connection but never signals
  end-of-stream; EventSource silently died after turn 1. Client now re-arms
  on every terminal frame with ?after_event_id resume cursor + maps terminal
  `error` type. Verified live (turn 2 first token 3.6s).
- Portrait vanish: no image-load error path + 2 nonexistent mai generic
  portrait ids (404s). Fail-closed revert + failed-frame exclusion + data fix.
- Mobile sprites: 2,680 WebP thumbnails (1000px q82, alpha) —
  **16.27 GiB → 506 MiB**; stage prefers thumbnailUrl with PNG fallback;
  endpoint allowlist extended to thumbs/ subpaths.

## Cleanup sweeps

- Pose `clasped` → `casual` (536 frames; 0004 is hands-low). Vignette mood
  tint fixed (JS/CSS class mismatch — never rendered). Stale auto-router
  copies tombstoned (canonical: hyrax-os/scripts). dagoth-ur dead admin ID
  removed (1448598625209811004 was a DM-channel snowflake, not a user).
- Housekeeping: feeder shadow path gated on mode (3,143-entry failure stream
  stops growing), discord-only lane composition journaling, daily counter
  prunes at 7d. 158 essenced tests green.

## Specs written (docs/gestalt-vn/ESSENCE_ACTIVE_RUNTIME.md)

- §17 upstream compatibility rule (Hermes stays stock; extension points only).
- §18-19 proactive outreach + Discord lane (as built C1-C3).
- §20 Phase D bounded autonomy: essenced as first caller of approve_proposal,
  free-tier lease classes (docs_wiki_notes, kanban_create), shadow-first,
  report-back mandatory, governor holds the leash.

## Context for next sessions

- **Whims layer (future)**: Sims-style object-wants ("text Josh about X",
  "organize Y") beyond the 3 templates. Per-operator decks from personality +
  events. Slow and small first.
- **Mai's LXC playground**: she already has a container + cron where she
  builds her own little tools — user loves watching it. This is the model
  first autonomy lease class: sandboxed, hers, zero blast radius. Whim:
  "I want to tinker" → her existing playground, not a cron.
- **Hermex app**: native iOS hermes-webui client (uzairansaruzi/hermex) —
  works against the fork for chat/sessions; VN/HQ layer is WebUI-only;
  outreach DMs land regardless. Future seam for native VN panels.
- **AIRI comparison**: companion/stage vs workforce-with-inner-lives;
  borrow voice (STT/TTS) and VRM stage polish someday.
- Open items: first organic fires tuning (watch outreach journal a week),
  rei's Discord 403 scope, matrix homeserver 502 storm, WebUI password
  rotation (plaintext in unit, 0.0.0.0 bind), discord-only lane asymmetry
  tests, 5xx+UA test gaps (done — see cleanup), Phase D1 when outreach
  proves out (~1 week of clean journals).
